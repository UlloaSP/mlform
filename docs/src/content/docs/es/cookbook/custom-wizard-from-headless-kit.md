---
title: Wizard Custom Con Headless Kit
description: Monta un wizard propio usando createFormView y un layout wizard.
---

Define `layout.kind = "wizard"`, crea el `view`, renderiza el step activo y conecta `nextStep()`, `prevStep()` y `submit()`.
