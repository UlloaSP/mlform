---
title: Formularios En Accordion
description: Usa el layout oficial accordion o el mismo config mediante un host propio.
---

Usa `mountAccordionForm()` para la shell integrada.

Usa `createFormView()` si el host debe controlar la experiencia de disclosure.
