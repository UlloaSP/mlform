---
title: Layout Headless En DOM Nativo
description: Renderiza nodos del snapshot directamente con DOM imperativo.
---

Renderiza recursivamente `snapshot.layout` y vuelve a pintar cuando `subscribe()` entregue un nuevo snapshot.
