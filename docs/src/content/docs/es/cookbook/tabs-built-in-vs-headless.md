---
title: Tabs Integrados Vs Headless
description: Decide entre mountForm y un host tabs propio con createFormView.
---

Usa `mountForm()` si quieres menos codigo y UI integrada.

Usa `createFormView()` si el producto ya tiene su propio sistema de tabs.
