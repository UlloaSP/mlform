---
title: Tabs Integrados Vs Headless
description: Decide entre mountTabsForm y un host tabs propio con createFormView.
---

Usa `mountTabsForm()` si quieres menos codigo y UI integrada.

Usa `createFormView()` si el producto ya tiene su propio sistema de tabs.
