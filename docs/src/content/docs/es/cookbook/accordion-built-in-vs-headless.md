---
title: Accordion Integrado Vs Headless
description: Decide entre mountAccordionForm y un host accordion propio con createFormView.
---

Usa `mountAccordionForm()` si quieres menos codigo y shell integrada.

Usa `createFormView()` si el producto ya tiene su propio sistema de disclosure o paneles.
