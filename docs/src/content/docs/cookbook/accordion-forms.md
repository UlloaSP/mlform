---
title: Disclosure Forms
description: Use the official disclosure layout or the same config through a custom host.
---

Disclosure is now an official built-in layout for progressive disclosure.

Use:

- `mountForm()` for the built-in shell
- `createFormView()` if the host must own disclosure behavior
- `layout.kind = "disclosure"` when multiple sections may stay open
