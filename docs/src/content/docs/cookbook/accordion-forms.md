---
title: Accordion Forms
description: Use the official accordion layout or the same config through a custom host.
---

Accordion is now an official built-in layout for progressive disclosure.

Use:

- `mountAccordionForm()` for the built-in shell
- `createFormView()` if the host must own disclosure behavior
- `layout.kind = "accordion"` when multiple sections may stay open
